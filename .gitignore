# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Audio files (keep recordings out of git)
*.m4a
*.mp3
*.wav
*.mp4
*.m4v
*.mov

# Transcription outputs (personal content)
[0-9][0-9]-[0-9][0-9]-[0-9][0-9].md

# API keys — never commit these
.env

# Personal config (vault names etc.) — copy config.example.json to config.json
config.json

# Whisper model cache
~/.cache/whisper/

# Git worktrees
.worktrees/

processed/
